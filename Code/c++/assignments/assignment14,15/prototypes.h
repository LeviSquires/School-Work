#include <sstream>
#include <string>
#include <iostream>
#include <vector>

int askForInt(std::string question, int high = INT_MAX, int low = INT_MIN);
bool operator==(const Date& d1, const Date& d2);
bool operator!=(const Date& d1, const Date& d2);
bool operator>(const Date& d1, const Date& d2);
bool operator>=(const Date& d1, const Date& d2);



